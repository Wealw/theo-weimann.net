<svg xmlns='http://www.w3.org/2000/svg' viewBox='15 20 110 110' style='aspect-ratio: 1' width="512px" height="512px">
    <style>
        path {
        stroke-dasharray: 100;
        stroke-dashoffset: 100;
        transform-box: fill-box;
        transform-origin: center;
        animation: dash 750ms linear forwards, spin 750ms ease-in-out 750ms forwards, opacity 200ms ease-out 1700ms forwards ;
        }

        @keyframes dash {
        to {
        stroke-dashoffset: 0;
        }
        }

        @keyframes opacity {
        to {
        opacity: 0;
        }
        }

        @keyframes spin {
        100% {
        transform:rotate(135deg);
        }
        }
    </style>
    <path class='path' stroke='#B90B64' stroke-width='12' stroke-linecap='round' fill='none' d='m75 75 m -37.5 12.5 v -25 a 1 1 0 0 1 25 0 v 25 m 0 -25 a 1 1 0 0 1 25 0 v 37.5 m -50 0 h 62.5' />
</svg>